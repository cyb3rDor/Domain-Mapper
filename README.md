# Domain-Mapper
Compact Bash tool for lab network reconnaissance and AD-aware enumeration on Kali: host discovery (nmap), TCP/UDP masscan, targeted nmap -sV, NSE domain checks, SMB/share/AD enumeration with crackmapexec and AS-REP roasting via impacket + john. Produces consolidated text report + PDF. Use only on authorized targets.
